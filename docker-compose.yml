version: '3.8'

services:
  reasoning-service:
    build: .
    container_name: reasoning-service
    ports:
      - "8080:8080"
    environment:
      - REASONING_OLLAMA_BASE_URL=http://192.168.0.140:11434
      - REASONING_OLLAMA_MODEL=deepseek-r1:14b
      - REASONING_MAX_REASONING_ITERATIONS=5
      - REASONING_MAX_CONTEXT_TOKENS=16000
      - REASONING_TEMPERATURE=0.7
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # For local development without Coolify
  # Uncomment to run Ollama locally (requires GPU)
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_models:/root/.ollama
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

# volumes:
#   ollama_models:
